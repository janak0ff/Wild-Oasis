import styled from "styled-components";

const Input = styled.input`
  padding: 10px 20px;
  border-radius: 1px solid var(--color-grey-300);
  border: none;
  cursor: pointer;
`;

export default Input;
